  font-size: 0.62rem;
  font-weight: 800;
  position: absolute;
  top: 50%;
  left: 50%;
}
.reel-spins {
  opacity: 0.7;
  margin: 0;
  font-size: 0.8rem;
  font-weight: 800;
}
.reel-wrap {
  width: min(100%, 260px);
  margin: 0.35rem auto;
}
.reel-window {
  background: #00000059;
  border: 3px solid #ffd16673;
  border-radius: 16px;
  height: 192px;
  position: relative;
  overflow: hidden;
  box-shadow:
    inset 0 0 24px #00000059,
    0 0 0 6px #ffd1661f;
}
.reel-strip {
  will-change: transform;
  backface-visibility: hidden;
}
.reel-cell {
  color: #07122a;
  text-shadow: 0 1px #ffffff59;
  border-bottom: 1px solid #0000001f;
  place-items: center;
  height: 64px;
  font-weight: 900;
  display: grid;
}
.reel-cell strong {
  letter-spacing: 0.02em;
  font-size: 1.05rem;
}
.reel-shade {
  z-index: 2;
  pointer-events: none;
  height: 64px;
  position: absolute;
  left: 0;
  right: 0;
}
.reel-shade.top {
  background: linear-gradient(#07122abf, #0000);
  top: 0;
}
.reel-shade.bot {
  background: linear-gradient(#0000, #07122abf);
  bottom: 0;
}
.reel-pointer {
  z-index: 1;
  pointer-events: none;
  border-top: 2px solid #ffd166d9;
  border-bottom: 2px solid #ffd166d9;
  height: 64px;
  margin-top: -32px;
  position: absolute;
  top: 50%;
  left: 0;
  right: 0;
  box-shadow: inset 0 0 20px #ffd16626;
}
.board-lose {
  filter: grayscale(0.35) brightness(0.85);
}
a[href*="netlify\.com"],
a[href*="Netlify"],
iframe[src*="netlify"],
#netlify-badge,
[id*="netlify-badge"],
[class*="netlify-badge"],
[data-netlify-deploy-id],
body > div[style*="z-index"][style*="fixed"] > a[href*="netlify"] {
  visibility: hidden !important;
  opacity: 0 !important;
  pointer-events: none !important;
  clip: rect(0, 0, 0, 0) !important;
  width: 0 !important;
  height: 0 !important;
  max-height: 0 !important;
  display: none !important;
  overflow: hidden !important;
}
.how-panel {
  padding-top: 0.5rem !important;
}
.how-top {
  align-items: center;
  gap: 0.55rem;
  margin-bottom: 0.15rem;
  display: flex;
}
.how-top h2 {
  margin: 0;
  font-size: 1.35rem;
}
.how-back {
  flex-shrink: 0;
}
.how-sub {
  margin-top: 0.15rem !important;
  margin-bottom: 0.5rem !important;
}
.how-panel .how-stage {
  background: #00000038;
  border: 1px solid #ffffff1a;
  border-radius: 18px;
  align-content: center;
  gap: 0.75rem;
  min-height: min(42dvh, 320px);
  margin: 0.25rem 0 0.75rem;
  padding: 0.85rem;
  display: grid;
}
.mini-board {
  aspect-ratio: 1;
  background: #07122ab3;
  border-radius: 14px;
  grid-template-rows: repeat(4, 1fr);
  grid-template-columns: repeat(4, 1fr);
  gap: 3px;
  width: min(100%, 220px);
  margin: 0 auto;
  padding: 6px;
  display: grid;
  position: relative;
  overflow: hidden;
}
.mini-cell {
  border-radius: 0;
  place-items: center;
  display: grid;
  position: relative;
}
.mini-fill {
  background: linear-gradient(
    155deg,
    hsl(var(--hue) 74% 64% / 0.95),
    hsl(var(--hue) 70% 52% / 0.88)
  );
  border-radius: 2px;
  position: absolute;
  inset: 0;
  box-shadow:
    inset 0 1px #ffffff59,
    0 1px 2px #0003;
}
.mini-buddy {
  z-index: 2;
  background: radial-gradient(circle at 30% 28%, #fff6cf, #ffd166 50%, #e09f1f);
  border-radius: 50%;
  width: 55%;
  height: 55%;
  animation:
    0.45s both miniPop,
    3.5s ease-in-out 0.5s infinite lookAround;
  position: relative;
  box-shadow: 0 2px 6px #00000059;
}
.mini-buddy.angry {
  background: radial-gradient(circle at 30% 28%, #ffd0d0, #ff6b6b 55%, #a61c1c);
