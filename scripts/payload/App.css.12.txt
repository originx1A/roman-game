  background: radial-gradient(circle at 35% 30%, #fff6cf, #ffd166 55%, #b45309);
  border-radius: 50%;
  width: 42px;
  height: 42px;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  box-shadow: 0 2px 8px #00000059;
}
.wheel-label {
  transform-origin: 50%;
  letter-spacing: 0.02em;
  color: #07122a;
  text-shadow: 0 1px #ffffff59;
  pointer-events: none;
  width: auto;
  margin-left: 0;
  padding: 0;
  font-size: 0.72rem;
  font-weight: 900;
  position: absolute;
  top: 50%;
  left: 50%;
}
.wheel-label em {
  background: #ffffff59;
  border-radius: 6px;
  padding: 0.15rem 0.35rem;
  font-style: normal;
  display: inline-block;
}
.wheel-pointer {
  border-left: 12px solid #0000;
  border-right: 12px solid #0000;
  border-top: 18px solid var(--sun);
  filter: drop-shadow(0 2px 4px #00000059);
  top: -4px;
}
.mini-hearts-demo,
.mini-critter-demo {
  background: #07122ab3;
  border-radius: 14px;
  place-items: center;
  gap: 0.5rem;
  width: min(100%, 220px);
  height: 140px;
  margin: 0 auto;
  display: grid;
}
.mini-hearts-demo {
  grid-auto-flow: column;
  justify-content: center;
}
.mini-hearts-label,
.mini-critter-label {
  opacity: 0.8;
  grid-column: 1/-1;
  font-size: 0.8rem;
  font-weight: 700;
}
.mini-critter-run {
  width: 48px;
  height: 48px;
  animation: 2.4s ease-in-out infinite miniCritterZip;
}
@keyframes miniCritterZip {
  0% {
    transform: translate(-60px, 8px);
  }
  50% {
    transform: translate(60px, -10px);
  }
  to {
    transform: translate(-60px, 8px);
  }
}
.lose-banner,
.win-banner {
  bottom: max(
    5.75rem,
    calc(5rem + env(safe-area-inset-bottom, 0px))
  ) !important;
}

/* —— Full-screen win celebration —— */
.win-screen {
  position: fixed;
  inset: 0;
  z-index: 50;
  display: grid;
  place-items: center;
  padding: max(1rem, env(safe-area-inset-top)) 1rem max(1rem, env(safe-area-inset-bottom));
  background:
    radial-gradient(ellipse 80% 55% at 50% 18%, #ffd16655, transparent 55%),
    radial-gradient(ellipse 70% 50% at 50% 100%, #3dffa838, transparent 50%),
    #030a1ef2;
  opacity: 0;
  transform: scale(1.04);
  transition: opacity 0.45s ease, transform 0.55s cubic-bezier(0.22, 1, 0.36, 1);
}
.win-screen.in {
  opacity: 1;
  transform: scale(1);
}
.win-screen-burst {
  pointer-events: none;
  position: absolute;
  inset: -10%;
  background:
    radial-gradient(circle at 20% 30%, #1a6dff44, transparent 28%),
    radial-gradient(circle at 80% 25%, #ffd16633, transparent 26%),
    radial-gradient(circle at 50% 80%, #3dffa822, transparent 35%);
  animation: 6s ease-in-out infinite winPulse;
}
@keyframes winPulse {
  0%,
  100% {
    transform: scale(1);
    opacity: 0.85;
  }
  50% {
    transform: scale(1.06);
    opacity: 1;
  }
}
.win-screen-sparkles {
  pointer-events: none;
  position: absolute;
  inset: 0;
  overflow: hidden;
}
.win-spark {
  --i: 0;
  position: absolute;
  left: calc(6% + (var(--i) * 5.1%));
  top: -12px;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ffd166;
  box-shadow: 0 0 10px #ffd166aa;
  animation: winFall 2.4s linear infinite;
  animation-delay: calc(var(--i) * 0.11s);
  opacity: 0.85;
}
.win-spark:nth-child(3n) {
  background: #3dffa8;
  box-shadow: 0 0 10px #3dffa8aa;
  width: 6px;
  height: 6px;
}
.win-spark:nth-child(4n) {
  background: #1a6dff;
  box-shadow: 0 0 10px #1a6dffaa;
}
.win-spark:nth-child(5n) {
  background: #ff6b6b;
  border-radius: 2px;
  width: 7px;
  height: 11px;
}
@keyframes winFall {
