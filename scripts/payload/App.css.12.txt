r: var(--mint);
  font-weight: 700;
}

.incoming-board {
  margin: 0.15rem 0 0.45rem;
  font-family: var(--font-display);
  font-size: 1.15rem;
  color: var(--sun, #ffd166);
  font-weight: 800;
}

.incoming-rank {
  display: inline-block;
  margin-top: 0.25rem;
  font-size: 0.88rem;
  color: rgba(244, 251, 255, 0.85);
}

.duel-board-tag {
  text-align: center;
  margin: 0 0 0.5rem;
  font-family: var(--font-display);
  font-size: 1.25rem;
  color: var(--sun, #ffd166);
  font-weight: 800;
}

.duel-rank-line {
  margin: 0.15rem 0 0.35rem;
  padding: 0 0.6rem;
  font-size: 0.78rem;
  opacity: 0.75;
  font-weight: 700;
}

.duel-card {
  margin: 0.85rem 0 1.1rem;
  padding: 1rem 1.05rem;
  border-radius: 18px;
  background: rgba(255, 255, 255, 0.06);
  border: 1px solid rgba(255, 255, 255, 0.12);
}

.duel-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 0.75rem;
  padding: 0.55rem 0.6rem;
  border-radius: 12px;
}

.duel-row.winner {
  background: rgba(61, 255, 168, 0.14);
  border: 1px solid rgba(61, 255, 168, 0.35);
}

.duel-name {
  font-weight: 800;
  color: var(--foam);
}

.duel-stats {
  font-family: var(--font-display);
  color: var(--mint);
  font-weight: 700;
}

.duel-vs {
  text-align: center;
  margin: 0.25rem 0;
  opacity: 0.55;
  font-weight: 800;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-size: 0.75rem;
}

.duel-verdict {
  text-align: center;
  margin: 0.75rem 0 0;
  font-family: var(--font-display);
  font-size: 1.2rem;
  color: var(--sun, #ffd166);
}

.hud-rival {
  font-size: 0.72rem;
  font-weight: 800;
  color: #ffd166;
  max-width: 9.5rem;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.result-burst {
  position: absolute;
  inset: 0;
  pointer-events: none;
  overflow: hidden;
}

.result-burst span {
  position: absolute;
  width: 8px;
  height: 8px;
  border-radius: 2px;
  background: var(--mint);
  opacity: 0;
  animation: resultSpark 1.1s ease-out both;
}

.result-burst span:nth-child(1) { left: 12%; top: 18%; animation-delay: 0.05s; background: #ffd166; }
.result-burst span:nth-child(2) { left: 78%; top: 22%; animation-delay: 0.12s; }
.result-burst span:nth-child(3) { left: 20%; top: 72%; animation-delay: 0.18s; background: #1a6dff; }
.result-burst span:nth-child(4) { left: 85%; top: 68%; animation-delay: 0.08s; background: #ff6b6b; }
.result-burst span:nth-child(5) { left: 48%; top: 8%; animation-delay: 0.22s; background: #c77dff; }
.result-burst span:nth-child(6) { left: 55%; top: 82%; animation-delay: 0.15s; background: #ffd166; }

.result-card-lose .result-burst span {
  background: #ff6b6b;
}

@keyframes resultScrimIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes resultCardIn {
  from { opacity: 0; transform: translateY(28px) scale(0.86); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}

@keyframes resultRomanIn {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes resultSpark {
  0% { opacity: 0; transform: translate(0, 0) scale(0.4) rotate(0deg); }
  35% { opacity: 1; }
  100% { opacity: 0; transform: translate(var(--dx, 20px), var(--dy, -40px)) scale(1.2) rotate(160deg); }
}

@media (prefers-reduced-motion: reduce) {
  .result-card,
  .result-scrim,
  .result-roman,
  .result-burst span {
    animation: none !important;
    opacity: 1;
    transform: none;
  }
}

/* —— Shortfall sheet (coin gap) —— */
.shortfall-overlay {
  position: fixed;
  inset: 0;
  z-index: 10050;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  padding: 0;
  pointer-events: auto;
}

.shortfall-scrim {
  position: absolute;
  inset: 0;
  border: 0;
  padding: 0;
  margin: 0;
  cursor: pointer;
  background: rgba(3, 8, 22, 0.62);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  animation: shortfallScrimIn 0.22s ease both;
}

.shortfall-sheet {
  position: relative;
  z-index: 1;
  width: 100%;
  max-width: 420px;
  margin: 0 auto;
  padding: 1.35rem 1.35rem calc(1.35rem + env(safe-area-inset-bottom, 0px));
  border-radius: var(--radius) var(--radius) 0 0;
  background: linear-gradient(165deg, #0e1f45 0%, var(--navy) 55%, #050d1f 100%);
  border: 1px solid rgba(255, 209, 102, 0.28);
  border-bottom: 0;
  box-shadow: 0 -18px 48px rgba(0, 0, 0, 0.45);
  animation: shortfallSlideUp 0.32s cubic-bezier(0.22, 1, 0.36, 1) both;
  display: grid;
  gap: 0.65rem;
}

.shortfall-accent {
  width: 2.5rem;
  height: 3px;
  