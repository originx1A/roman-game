  animation: 0.5s infinite shake;
}
.mini-eye {
  background: #07122a;
  border-radius: 50%;
  width: 18%;
  height: 22%;
  position: absolute;
  top: 36%;
}
.mini-eye.l {
  left: 26%;
}
.mini-eye.r {
  right: 26%;
}
.mini-x {
  z-index: 2;
  color: #fff8e6e6;
  font-size: 0.95rem;
  font-weight: 900;
  animation: 0.35s both miniMark;
  position: relative;
}
.mini-finger {
  z-index: 4;
  background: #ffffffd9;
  border-radius: 50% 50% 50% 10%;
  width: 18px;
  height: 18px;
  animation: 2.2s ease-in-out infinite swipeFinger;
  position: absolute;
  box-shadow: 0 0 0 4px #ffd16659;
}
.mini-zap {
  pointer-events: none;
  z-index: 3;
  border: 2px solid #ff6b6b;
  border-radius: 4px;
  animation: 1s ease-in-out infinite pulseZap;
  position: absolute;
  inset: -10%;
}
.mini-scan {
  pointer-events: none;
  z-index: 3;
  background: #ffd16647;
  border-radius: 4px;
  position: absolute;
}
.mini-scan.row {
  height: 22%;
  animation: 2.8s ease-in-out infinite scanRow;
  top: 6px;
  left: 6px;
  right: 6px;
}
.mini-scan.col {
  width: 22%;
  animation: 2.8s ease-in-out infinite scanCol;
  top: 6px;
  bottom: 6px;
  left: 6px;
}
.mini-region-glow {
  pointer-events: none;
  z-index: 3;
  border: 2px solid #ffd166b3;
  border-radius: 6px;
  animation: 2s ease-in-out infinite regionPulse;
  position: absolute;
  inset: 8% 52% 52% 8%;
}
.demo-swipe .mini-x {
  animation: 0.4s both miniMark;
}
.demo-swipe .has-mark:nth-child(2) .mini-x {
  animation-delay: 0.15s;
}
.demo-swipe .has-mark:nth-child(3) .mini-x {
  animation-delay: 0.3s;
}
.demo-swipe .has-mark:nth-child(4) .mini-x {
  animation-delay: 0.45s;
}
.how-caption h3 {
  font-family: var(--font-display);
  color: var(--sun);
  margin: 0.15rem 0;
  font-size: 1.35rem;
}
.how-caption p {
  opacity: 0.85;
  margin: 0;
  font-weight: 650;
}
.how-step-label {
  letter-spacing: 0.12em;
  text-transform: uppercase;
  opacity: 0.55;
  font-size: 0.75rem;
  font-weight: 800;
}
.how-dots {
  justify-content: center;
  gap: 0.45rem;
  margin-bottom: 1rem;
  display: flex;
}
.how-dots button {
  cursor: pointer;
  background: #ffffff40;
  border: 0;
  border-radius: 50%;
  width: 10px;
  height: 10px;
  padding: 0;
}
.how-dots button.on {
  background: var(--sun);
  box-shadow: 0 0 10px #ffd16680;
}
.how-rules {
  margin-top: 0.25rem;
}
@keyframes miniPop {
  0% {
    opacity: 0;
    transform: scale(0.4);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
@keyframes miniMark {
  0% {
    opacity: 0;
    transform: scale(0.5);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
@keyframes swipeFinger {
  0% {
    opacity: 0;
    top: 28%;
    left: 8%;
  }
  15% {
    opacity: 1;
  }
  55% {
    opacity: 1;
    top: 28%;
    left: 72%;
  }
  75%,
  to {
    opacity: 0;
    top: 28%;
    left: 72%;
  }
}
@keyframes scanRow {
  0%,
  to {
    top: 8%;
  }
  50% {
    top: 70%;
  }
}
@keyframes scanCol {
  0%,
  to {
    left: 8%;
  }
  50% {
    left: 70%;
  }
}
@keyframes regionPulse {
  0%,
  to {
    opacity: 0.45;
  }
  50% {
    opacity: 1;
  }
}
@keyframes pulseZap {
  0%,
  to {
    opacity: 0.35;
  }
  50% {
    opacity: 1;
  }
}
.buddy-accent {
  display: none;
}
.buddy-cosmic .buddy-body {
  background: radial-gradient(
    circle at 30% 28%,
    #f3e8ff,
    #a78bfa 45%,
    #4c1d95 100%
  );
}
.buddy-cosmic .buddy-accent {
  z-index: -1;
  background: radial-gradient(circle, #a78bfa73, #0000 70%);
  border-radius: 50%;
  animation: 2.4s ease-in-out infinite auraPulse;
