  font-size: clamp(1.35rem, 4vw, 1.85rem) !important;
}
.toolbar {
  flex-shrink: 0;
  padding-bottom: 0.25rem;
  margin-top: 0.55rem !important;
}
.banter-inline,
.foot-hidden {
  display: none !important;
}
.netlify-safe {
  height: calc(64px + env(safe-area-inset-bottom, 0px));
  pointer-events: none;
  z-index: 5;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
}
a[href*="netlify\.com"],
a[href*="Netlify"],
a[aria-label*="Netlify"],
iframe[src*="netlify"],
#netlify-badge,
[id*="netlify-badge"],
[class*="netlify-badge"],
[class*="Netlify"],
[data-netlify-deploy-id],
body > a[style*="fixed"],
body > div > a[href*="netlify"] {
  visibility: hidden !important;
  opacity: 0 !important;
  pointer-events: none !important;
  clip: rect(0, 0, 0, 0) !important;
  width: 0 !important;
  height: 0 !important;
  max-height: 0 !important;
  display: none !important;
  position: fixed !important;
  bottom: auto !important;
  left: -9999px !important;
  overflow: hidden !important;
}
.hud-pills {
  gap: 0.4rem;
  display: flex;
}
.score-pill.coin {
  color: var(--sun);
  background: #ffd1661f;
  border-color: #ffd16666;
}
.banter {
  display: none !important;
}
.lives {
  align-items: center;
  gap: 0.25rem;
  display: inline-flex;
}
.heart {
  clip-path: polygon(
    50% 92%,
    8% 55%,
    8% 32%,
    22% 18%,
    38% 18%,
    50% 30%,
    62% 18%,
    78% 18%,
    92% 32%,
    92% 55%
  );
  background: #fff3;
  width: 0.85rem;
  height: 0.85rem;
  transition:
    transform 0.2s,
    background 0.2s,
    opacity 0.2s;
  display: inline-block;
  position: relative;
}
.heart.on {
  background: #ff6b6b;
  box-shadow: 0 0 8px #ff6b6b8c;
}
.heart.pop {
  background: #ff6b6b;
  animation: 0.65s forwards heartLose;
}
@keyframes heartLose {
  0% {
    opacity: 1;
    filter: none;
    transform: scale(1);
  }
  35% {
    opacity: 1;
    filter: brightness(1.3);
    transform: scale(1.45) rotate(-12deg);
  }
  70% {
    opacity: 0.5;
    transform: scale(0.6) translateY(8px) rotate(18deg);
  }
  to {
    opacity: 0;
    background: #fff3;
    transform: scale(0.2) translateY(16px);
  }
}
.buddy-mouth {
  opacity: 0.7;
  background: 0 0;
  border-bottom: 2px solid #50280a73;
  border-radius: 0 0 50% 50%;
  width: 22%;
  height: 10%;
  margin-left: -11%;
  position: absolute;
  top: 62%;
  left: 50%;
}
.buddy.giggle .buddy-mouth {
  background: #50280a8c;
  border-bottom-width: 0;
  border-radius: 40%;
  height: 16%;
  top: 60%;
}
.buddy.giggle .buddy-eye {
  border-radius: 40%;
  height: 14%;
  top: 40%;
}
.buddy.giggle .buddy-pupil {
  display: none;
}
.giggle-burst {
  pointer-events: none;
  z-index: 4;
  font-size: 0.55rem;
  animation: 0.85s forwards giggleFloat;
  position: absolute;
}
.giggle-burst.a {
  animation-delay: 50ms;
  top: 10%;
  left: -10%;
}
.giggle-burst.c {
  color: var(--sun);
  animation-delay: 0.18s;
  top: -18%;
  left: 40%;
}
@keyframes giggleFloat {
  0% {
    opacity: 0;
    transform: translateY(4px) scale(0.6);
  }
  30% {
    opacity: 1;
  }
  to {
    opacity: 0;
    transform: translateY(-18px) scale(1.1);
  }
}
.lose-banner,
.win-banner {
  left: 50%;
  bottom: max(5.75rem, calc(5rem + env(safe-area-inset-bottom, 0px)));
  z-index: 40;
  box-sizing: border-box;
  clip-path: none;
  text-align: center;
  border-radius: 16px;
  width: min(92vw, 340px);
  max-width: calc(100vw - 1.25rem);
  margin: 0;
  padding: 0.85rem 1rem;
  position: fixed;
  overflow: visible;
  transform: translate(-50%);
}
.win-banner {
