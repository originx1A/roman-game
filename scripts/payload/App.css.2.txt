  font-weight: 650;
  line-height: 1.75;
}
.diff-block {
  margin-bottom: 1.5rem;
}
.diff-block h3 {
  letter-spacing: 0.14em;
  text-transform: uppercase;
  opacity: 0.55;
  margin: 0 0 0.7rem;
  font-size: 0.85rem;
  font-weight: 800;
}
.level-grid {
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 0.65rem;
  display: grid;
}
.level-card {
  text-align: left;
  color: var(--foam);
  cursor: pointer;
  background: #f4fbff0f;
  border: 1px solid #ffffff1f;
  border-radius: 16px;
  padding: 0.95rem;
  font-weight: 700;
  transition:
    transform 0.18s,
    border-color 0.18s,
    background 0.18s;
}
.level-card:hover {
  background: #ffd1661a;
  border-color: #ffd1668c;
  transform: translateY(-3px) rotate(-0.4deg);
}
.level-card.cleared {
  border-color: #3dffa880;
  box-shadow: inset 0 0 0 1px #3dffa833;
}
.lv-name {
  margin-bottom: 0.25rem;
  font-size: 1.05rem;
  display: block;
}
.lv-meta {
  opacity: 0.65;
  font-size: 0.8rem;
}
.play-meta {
  justify-content: space-between;
  align-items: end;
  gap: 1rem;
  margin-bottom: 1rem;
  display: flex;
}
.meters {
  font-variant-numeric: tabular-nums;
  opacity: 0.9;
  flex-wrap: wrap;
  gap: 0.75rem;
  font-weight: 800;
  display: flex;
}
.win-score {
  color: var(--sun);
}
.board {
  --gap: 3px;
  grid-template-columns: repeat(var(--n), 1fr);
  grid-template-rows: repeat(var(--n), 1fr);
  gap: var(--gap);
  width: min(100%, min(520px, 52dvh));
  max-height: min(520px, 52dvh);
  box-shadow: var(--shadow);
  aspect-ratio: 1;
  touch-action: none;
  -webkit-user-select: none;
  user-select: none;
  background: #040a1cb8;
  border: 2px solid #ffd16638;
  border-radius: 16px;
  flex-shrink: 1;
  margin: 0 auto;
  padding: 0.45rem;
  display: grid;
}
.cell {
  cursor: pointer;
  aspect-ratio: 1;
  z-index: 1;
  will-change: transform;
  background: 0 0;
  border: 0;
  border-radius: 0;
  place-items: center;
  width: 100%;
  height: 100%;
  padding: 0;
  transition: transform 0.14s cubic-bezier(0.22, 1, 0.36, 1);
  display: grid;
  position: relative;
  overflow: visible;
}
.cell-fill {
  background: linear-gradient(
    155deg,
    hsl(var(--hue) var(--sat, 72%) calc(var(--lit, 56%) + 10%) / 0.95) 0%,
    hsl(var(--hue) var(--sat, 72%) var(--lit, 56%) / 0.88) 42%,
    hsl(
        var(--hue) calc(var(--sat, 72%) - 6%) calc(var(--lit, 56%) - 12%) / 0.92
      )
      100%
  );
  border-radius: 3px;
  transition:
    filter 0.14s,
    box-shadow 0.14s,
    transform 0.14s cubic-bezier(0.22, 1, 0.36, 1);
  position: absolute;
  inset: 0;
  box-shadow:
    inset 0 1.5px #ffffff61,
    inset 0 -2px 5px #0000002e,
    0 1px 2px #0000002e;
}
.cell:hover .cell-fill {
  filter: brightness(1.05);
}
.cell.pressed {
  z-index: 4;
  transform: translateY(-3px) scale(1.045);
}
.cell.pressed .cell-fill {
  filter: brightness(1.1);
  box-shadow:
    inset 0 2px #ffffff80,
    inset 0 -3px 6px #00000038,
    0 8px 16px #00000061,
    0 3px 6px #00000038,
    0 0 0 1px #ffffff1f;
}
.cell.edge-t {
  box-shadow: inset 0 2px #07122aa6;
}
.cell.edge-b {
  box-shadow: inset 0 -2px #07122aa6;
}
.cell.edge-l {
  box-shadow: inset 2px 0 #07122aa6;
}
.cell.edge-r {
  box-shadow: inset -2px 0 #07122aa6;
}
.cell.edge-t .cell-fill {
  border-top: 2px solid #07122a73;
}
.cell.edge-b .cell-fill {
  border-bottom: 2px solid #07122a73;
}
.cell.edge-l .cell-fill {
  border-left: 2px solid #07122a73;
}
.cell.edge-r .cell-fill {
  border-right: 2px solid #07122a73;
}
.mark-x {
  z-index: 2;
