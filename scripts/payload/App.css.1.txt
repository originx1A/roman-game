  background: var(--sun);
}
.score-pill {
  color: var(--mint);
  background: #3dffa81f;
  border: 1px solid #3dffa859;
  border-radius: 999px;
  align-items: baseline;
  gap: 0.3rem;
  padding: 0.4rem 0.8rem;
  font-weight: 800;
  display: inline-flex;
}
.score-pill span {
  opacity: 0.75;
  font-size: 0.75rem;
  font-weight: 700;
}
.home,
.panel,
.play {
  max-width: 980px;
  margin: 0 auto;
  padding: 0.5rem 1.25rem 5rem;
}
.hero {
  flex-direction: column;
  justify-content: flex-end;
  min-height: min(72dvh, 660px);
  padding: 2rem 0 1.5rem;
  animation: 0.75s both rise;
  display: flex;
}
.dedication {
  width: fit-content;
  color: var(--sun);
  letter-spacing: 0.04em;
  text-transform: uppercase;
  background: #ffd16624;
  border: 1px solid #ffd16659;
  border-radius: 999px;
  align-items: center;
  gap: 0.45rem;
  margin-bottom: 0.85rem;
  padding: 0.35rem 0.75rem;
  font-size: 0.85rem;
  font-weight: 800;
  animation: 0.7s both rise;
  display: inline-flex;
}
.dedication-star {
  background: var(--sun);
  clip-path: polygon(
    50% 0%,
    61% 35%,
    98% 35%,
    68% 57%,
    79% 91%,
    50% 70%,
    21% 91%,
    32% 57%,
    2% 35%,
    39% 35%
  );
  width: 0.65rem;
  height: 0.65rem;
}
.eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: #f4fbff8c;
  margin: 0 0 0.65rem;
  font-size: 0.78rem;
  font-weight: 800;
}
.logo-hero {
  font-family: var(--font-display);
  letter-spacing: 0.02em;
  color: var(--foam);
  text-shadow:
    0 6px #1a6dff,
    0 14px 40px #00000059;
  margin: 0;
  font-size: clamp(4.4rem, 16vw, 8rem);
  line-height: 0.88;
  animation: 4.5s ease-in-out infinite floaty;
}
.lede {
  color: #f4fbffd1;
  max-width: 28rem;
  margin: 1.15rem 0 1.55rem;
  font-size: 1.15rem;
  font-weight: 600;
  line-height: 1.5;
}
.cta-row {
  flex-wrap: wrap;
  gap: 0.7rem;
  display: flex;
}
.btn {
  cursor: pointer;
  border: 0;
  border-radius: 999px;
  padding: 0.9rem 1.4rem;
  font-weight: 800;
  transition:
    transform 0.18s,
    box-shadow 0.18s,
    background 0.18s;
}
.btn:active {
  transform: translateY(1px) scale(0.98);
}
.btn.primary {
  background: linear-gradient(135deg, #ffe08a, var(--sun) 40%, #ff9f1c);
  color: #1a1200;
  box-shadow: 0 12px 28px #ffd16659;
}
.btn.primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 16px 34px #ffd16673;
}
.btn.ghost {
  color: var(--foam);
  background: #ffffff14;
  border: 1px solid #ffffff29;
}
.btn.tool {
  color: var(--foam);
  background: #07122aa6;
  border: 1px solid #ffffff1f;
  padding: 0.7rem 1rem;
}
.btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
.home-strip {
  grid-template-columns: repeat(3, 1fr);
  gap: 0.75rem;
  margin-top: 1.5rem;
  animation: 0.85s 80ms both rise;
  display: grid;
}
.home-strip div {
  border-radius: var(--radius);
  background: var(--panel);
  border: 1px solid #ffffff1a;
  flex-direction: column;
  gap: 0.25rem;
  padding: 1rem;
  display: flex;
}
.home-strip strong {
  font-family: var(--font-display);
  color: var(--sun);
  font-size: 1.7rem;
}
.home-strip span {
  opacity: 0.7;
  font-size: 0.88rem;
  font-weight: 700;
}
.panel {
  animation: 0.45s both rise;
}
.panel h2,
.play h2 {
  font-family: var(--font-display);
  color: var(--sun);
  margin: 0.2rem 0 0.4rem;
  font-size: clamp(2.1rem, 6vw, 3rem);
}
.sub {
  opacity: 0.75;
  max-width: 36rem;
  margin: 0 0 1.4rem;
  font-weight: 600;
  line-height: 1.5;
}
.rules {
  max-width: 34rem;
  margin: 0 0 1.5rem;
  padding-left: 1.2rem;
