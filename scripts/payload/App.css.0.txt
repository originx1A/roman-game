/* Recovered from https://roman-game.surge.sh/assets/index-BBtjTAe1.css (prettier).
 * This is the production-bundled stylesheet (includes src/index.css reset + src/App.css).
 * Not identical to original src/App.css source formatting/comments.
 */

* {
  margin: 0;
}
:root {
  --ink: #07122a;
  --foam: #f4fbff;
  --sky: #1a6dff;
  --sun: #ffd166;
  --mint: #3dffa8;
  --coral: #ff6b6b;
  --panel: #f4fbff14;
  --shadow: 0 22px 60px #030a1e73;
  --radius: 22px;
  --font-display: "Titan One", "Fredoka", system-ui, sans-serif;
  --font-body: "Nunito", system-ui, sans-serif;
}
* {
  box-sizing: border-box;
}
html,
body,
#root {
  min-height: 100%;
}
body {
  font-family: var(--font-body);
  color: var(--foam);
  -webkit-font-smoothing: antialiased;
  background: #050d1f;
  margin: 0;
}
button,
input,
select {
  font: inherit;
}
.shell {
  min-height: 100dvh;
  position: relative;
  overflow-x: hidden;
}
.atmosphere {
  z-index: 0;
  pointer-events: none;
  background:
    radial-gradient(900px 600px at 12% -8%, #1a6dff8c, #0000 58%),
    radial-gradient(700px 500px at 88% 8%, #ffd16652, #0000 52%),
    radial-gradient(800px 520px at 50% 110%, #3dffa838, #0000 48%),
    linear-gradient(168deg, #07122a 0%, #0a1a3d 45%, #10285a 100%);
  position: fixed;
  inset: 0;
}
.atmosphere:before {
  content: "";
  opacity: 0.9;
  background-image:
    radial-gradient(2px 2px at 12% 18%, #ffffffb3, #0000),
    radial-gradient(2px 2px at 28% 42%, #ffffff73, #0000),
    radial-gradient(1.5px 1.5px at 63% 22%, #ffffff8c, #0000),
    radial-gradient(2px 2px at 78% 58%, #fff6, #0000),
    radial-gradient(1.5px 1.5px at 41% 72%, #ffffff80, #0000),
    radial-gradient(2px 2px at 91% 31%, #ffd166b3, #0000);
  animation: 5s ease-in-out infinite twinkle;
  position: absolute;
  inset: 0;
}
.atmosphere:after {
  content: "";
  pointer-events: none;
  background:
    radial-gradient(at 50% 0, #1a6dff40, #0000 60%),
    linear-gradient(#0000, #050d1fe6);
  height: 42%;
  position: absolute;
  inset: auto -10% -18%;
}
.topbar,
main,
.foot,
.toast {
  z-index: 1;
  position: relative;
}
.topbar {
  flex-shrink: 0;
  align-items: center;
  gap: 0.55rem;
  max-width: 980px;
  margin: 0 auto;
  padding: 0.4rem 0.85rem;
  display: flex;
}
.topbar-slim .brand-name {
  font-size: 1.05rem;
}
.topbar-slim .brand-mark {
  width: 0.95rem;
  height: 0.95rem;
}
.topbar-slim .nav button {
  padding: 0.28rem 0.55rem;
  font-size: 0.82rem;
}
.topbar-slim .score-pill {
  padding: 0.28rem 0.55rem;
  font-size: 0.85rem;
}
.shell-immersive .topbar {
  display: none !important;
}
.brand {
  color: var(--foam);
  cursor: pointer;
  background: 0 0;
  border: 0;
  align-items: center;
  gap: 0.55rem;
  padding: 0;
  display: inline-flex;
}
.brand-mark {
  background: radial-gradient(
    circle at 35% 30%,
    #fff6cf,
    var(--sun) 45%,
    #f4a261 100%
  );
  border-radius: 50%;
  width: 1.25rem;
  height: 1.25rem;
  animation: 2.8s ease-in-out infinite breath;
  box-shadow:
    0 0 0 3px #ffd16647,
    0 0 18px #ffd16673;
}
.brand-name {
  font-family: var(--font-display);
  letter-spacing: 0.02em;
  color: var(--sun);
  text-shadow: 0 2px #00000040;
  font-size: 1.45rem;
}
.nav {
  flex-wrap: wrap;
  gap: 0.35rem;
  margin-left: auto;
  display: flex;
}
.nav button {
  color: #f4fbffb8;
  cursor: pointer;
  background: 0 0;
  border: 0;
  border-radius: 999px;
  padding: 0.45rem 0.75rem;
  font-weight: 700;
}
.nav button.on,
.nav button:hover {
  color: #07122a;
