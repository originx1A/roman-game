  0% {
    transform: translateY(0) rotate(0deg);
    opacity: 0;
  }
  12% {
    opacity: 1;
  }
  100% {
    transform: translateY(110vh) rotate(420deg);
    opacity: 0.15;
  }
}
.win-screen-card {
  position: relative;
  z-index: 1;
  width: min(92vw, 420px);
  max-height: min(92dvh, 720px);
  overflow: auto;
  text-align: center;
  padding: 1.35rem 1.25rem 1.2rem;
  border-radius: 28px;
  background: linear-gradient(165deg, #0d1f44f5 0%, #07122af7 55%, #0a1838f5 100%);
  border: 1px solid #ffd16655;
  box-shadow:
    0 0 0 1px #3dffa822 inset,
    0 28px 60px #000000a6;
  animation: 0.55s cubic-bezier(0.22, 1, 0.36, 1) both winCardIn;
}
@keyframes winCardIn {
  from {
    opacity: 0;
    transform: translateY(28px) scale(0.94);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
.win-screen-kicker {
  margin: 0;
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #f4fbffb3;
}
.win-screen-title {
  margin: 0.35rem 0 0.15rem;
  font-family: var(--font-display);
  font-size: clamp(2.4rem, 11vw, 3.4rem);
  line-height: 1;
  color: var(--sun);
  text-shadow: 0 4px 0 #c48900, 0 10px 28px #ffd16655;
  animation: 0.7s cubic-bezier(0.34, 1.4, 0.64, 1) 0.12s both winTitlePop;
}
@keyframes winTitlePop {
  from {
    opacity: 0;
    transform: scale(0.6) rotate(-4deg);
  }
  to {
    opacity: 1;
    transform: scale(1) rotate(0);
  }
}
.win-screen-board {
  margin: 0 0 0.85rem;
  font-weight: 800;
  font-size: 1.05rem;
  color: var(--foam);
}
.win-latin {
  margin: 0 0 0.75rem;
  padding: 0.7rem 0.85rem;
  border-radius: 16px;
  background: #ffffff0f;
  border: 1px solid #ffd16633;
}
.win-latin-line {
  margin: 0;
  font-family: var(--font-display);
  font-size: clamp(1.05rem, 4.2vw, 1.35rem);
  color: var(--mint);
  line-height: 1.2;
}
.win-latin-gloss {
  display: block;
  margin-top: 0.35rem;
  font-style: normal;
  font-size: 0.78rem;
  font-weight: 700;
  color: #f4fbff99;
}
.win-roman-says {
  margin: 0 0 0.85rem;
  padding: 0.65rem 0.75rem;
  border-radius: 14px;
  background: #1a6dff24;
  border: 1px solid #1a6dff55;
  font-weight: 800;
  font-size: clamp(0.92rem, 3.6vw, 1.05rem);
  line-height: 1.35;
  color: var(--foam);
  animation: 0.5s ease 0.35s both rise;
}
.win-perfect {
  margin: -0.35rem 0 0.85rem;
  font-weight: 800;
  font-size: 0.82rem;
  color: var(--sun);
}
.win-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.45rem;
  margin: 0 0 1rem;
}
.win-stat {
  padding: 0.55rem 0.35rem;
  border-radius: 14px;
  background: #ffffff12;
  border: 1px solid #ffffff1f;
}
.win-stat:nth-child(4),
.win-stat:nth-child(5) {
  grid-column: span 1;
}
.win-stats .win-stat:nth-child(4) {
  grid-column: 1 / 2;
}
.win-stats .win-stat:nth-child(5) {
  grid-column: 2 / 3;
}
.win-stat-label {
  display: block;
  font-size: 0.65rem;
  font-weight: 800;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: #f4fbff8c;
  margin-bottom: 0.2rem;
}
.win-stat-value {
  font-family: var(--font-display);
  font-size: 1.15rem;
  color: var(--foam);
  font-weight: 400;
}
.win-screen-actions {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.win-cta {
  width: 100%;
  justify-content: center;
}
.win-screen-actions .btn.primary.win-cta + .btn.primary.win-cta {
  background: linear-gradient(180deg, #ffe08a, #ffd166);
  color: #07122a;
  border-color: #ffd166;
}
.win-screen-secondary {
  display: flex;
  gap: 0.45rem;
  margin-top: 0.15rem;
}
